# 互動測試紀錄

已在開發預覽載入第一組段落題，確認 10 個缺字皆呈現為可聚焦、可輸入的獨立欄位，且每一欄的可見字首與輸入長度相符。已填入第一組的正確缺失字母，準備驗證「提交並批改」後的分數、完成狀態與 localStorage 進度更新。

已確認「提交並批改」按鈕在瀏覽器中保持可用，並以實際頁面事件提交第一組答案。下一步需讀取畫面上的批改結果與進度數值，確認結果與輸入答案一致。

端到端結果：第一組正確作答後，畫面顯示「10/10 已校對」與全對提示；本輪稿件更新為 1/10、總答題數更新為 10、累積正確率為 100%、今日目標更新為 1/2 組與 10/20 題，待複習維持 0。題組導覽也正確標示第一組為已作答，並顯示「下一組」按鈕。

差異化 Cobalt Signal Console 改版回歸：第一組的 10 個缺字欄位仍可獨立聚焦並接受正確缺失字母；可見字首、最大輸入長度及題組導覽均正確呈現。下一步將提交以確認新版資料塔與完成狀態會同步更新。

已透過差異化版的 `CHECK SIGNAL` 頁面按鈕觸發第一組提交；待讀取結果確認分數、完成狀態與資料塔計數完全同步。

回歸結果：差異化版正確顯示 `10/10 CHECKED` 與 `CLEAN SIGNAL`；Round 更新為 1/10 solved，資料塔顯示 100% accuracy、010 attempts、00 review sets、today 1/2。`NEXT` 控制項也在批改後出現，確認核心填空、計分、localStorage 進度與新版介面正常整合。
