# 每兩組提醒端到端驗證紀錄

## 測試起點

- 日期：2026-08-20。
- 本機進度設定為：同日已完成 1 組、`pendingDailyReminderAt = 0`、無待複習題組。
- 題面實際顯示：`DAY 01 / 02` 與「已完成 1/2 組，還差 1 組」。

## 已完成互動流程

1. 在完整十空段落題面按下 `CHECK PASSAGE`。
2. 頁面實際變為 `0/10 CHECKED`，並出現 `REVIEW THIS PASSAGE`。
3. 此流程已產生該段的 10 個錯題，待續以檢討頁底部的跳過操作推進至下一段，才會完成第 2 組並驗證提醒。

4. 在檢討頁實際使用置底 `SKIP REVIEW · NEXT PASSAGE／跳過本次檢討`。
5. 下一段題面實際顯示 `DAY 02 · NEXT 04`、`今天已完成 2 組`，以及「再完成 2 組會再次提醒」。
6. 頁面同時顯示三個可見選項：`繼續下一組`、`複習高頻錯題（再練習一次今天錯比較多的題組）`、`今天先到這裡`。
7. 在提醒仍顯示時重新載入頁面後，題面仍為第 19 組，並再次顯示 `DAY 02 · NEXT 04`、每日提示與完整三選一。
8. 實際點選 `繼續下一組` 後，提醒區消失；題面保留可作答狀態與 `DAY 02 · NEXT 04`，可正常開始第 3 組。
9. 已在第 3 組實際點選 `CHECK PASSAGE`，待進入檢討並推進至第 4 組。
10. 第 3 組已依序完成 `CHECK PASSAGE → REVIEW THIS PASSAGE → SKIP REVIEW · NEXT PASSAGE`。
11. 頁面已實際到達第 4 組題面，顯示 `DAY 03 · NEXT 04` 與「再完成 1 組會再次提醒」。
12. 第 4 組以實際 `CHECK PASSAGE → REVIEW THIS PASSAGE → SKIP REVIEW · NEXT PASSAGE` 完成後，下一題面顯示 `DAY 04 · NEXT 06`。
13. 第 4 組提醒區實際顯示「今日已完成 4 組」及完整三選一：繼續下一組、複習高頻錯題、今天先到這裡。
14. 點選第 4 組提醒的 `繼續下一組` 後，提醒正常關閉，題面保持可作答。
15. 第 5 組已依真實流程完成並到達第 6 組題面；頁面顯示 `DAY 05 · NEXT 06` 及「再完成 1 組會再次提醒」。
16. 第 6 組以實際 `CHECK PASSAGE → REVIEW THIS PASSAGE → SKIP REVIEW · NEXT PASSAGE` 完成後，頁面顯示 `DAY 06 · NEXT 08`。
17. 第 6 組提醒區實際顯示「今日已完成 6 組」與完整三選一：繼續下一組、複習高頻錯題、今天先到這裡。
18. 點選第 6 組提醒的 `繼續下一組` 後，提醒區消失；已插入的高錯複習題仍保持在可作答狀態，未被提醒操作中斷。

> 第 2、4、6 組均已由真正的「段落提交 → 錯題檢討 → 跳過／推進下一段」互動流程驗證，並非透過直接寫入完成數取得畫面。

> 第 2 組提醒已由完整頁面流程及重新載入驗證。下一步以相同的實際提交與檢討流程驗證第 4 與第 6 組。
