# 檢討流程測試紀錄

在開發預覽中以空白答案依序提交十組題目，並在每組批改後前往下一組。第十組批改完成後，頁面成功自動切換至 `ROUND COMPLETE` 檢討畫面，確認一輪完成判定與自動轉場已正常運作。

已清除測試進度後重新跑完十組題目，以驗證每日完成組數僅在整輪結束時增加。自動檢查回傳的顯示值未符合預期，需要讀取檢討畫面的實際內容以定位是否為畫面文案或狀態更新時序問題。

檢討頁實際列出每一個錯誤空格的學生輸入、完整正解、答案位置與原段落；`RETRY MISSED` 成功將錯題所在的十個題組重新載入為可作答題組。最後直接核對本機進度，`todayGroups` 在一輪十組完成後為 `1`，確認每日目標不會逐題累加。

單題拼寫模式重構後：以錯誤拼字提交第 1 題，原完整句子中立即顯示正解並標示已加入錯題本；按下 `NEXT QUESTION` 後，畫面成功切換為下一個完整句子與下一個單字的拼寫欄位。這與參考網站的逐題作答節奏一致。

自動完成十題後，系統顯示回合摘要，提供 `REVIEW MISSED WORDS` 與 `START NEXT ROUND`。按下錯題複習後，畫面成功回到單題拼寫介面，第一題即為錯題的完整句子與單字拼寫欄位，而非整批答案列表。

兩階段修正後，以一段十空題提交空白答案，再選擇檢討；檢討模式正確只顯示 **10 個**該段錯字，且每一題均是「完整句子＋一個單字拼寫」的形式，不再將十段題目的 100 個錯字堆在同一個檢討清單。

逐一完成上述十題錯字檢討後，系統回到第 2 段的十空 Complete the Words 考試題面，確認流程為「一段考試 → 該段最多十題逐句檢討 → 下一段考試」。

大小寫與十次拼寫驗證：已掃描並修正 256 個句中不當大寫，保留 DNA、Earth、Renaissance 等必要縮寫或專名。第 2 段的可見字首現為 `evol / gene / tis / or / hab / ecos`，不是大寫。以一個錯詞 `the` 測試時，第一次正確為 1/10；輸入錯誤後計數回到 0/10；再以純鍵盤 Enter 連續正確輸入十次，系統自動前往下一段十空題面，未需滑鼠操作。

自由拼寫修正後，以同一錯詞 `the` 測試：正確輸入後，答案會依序填入 1–10 練習格；首次與第二次均正確記錄為第 1、2 格。輸入欄會保持鍵盤焦點，學生可隨時選擇 `RETURN TO PASSAGE`，而不必寫滿十次；在僅完成兩次後手動返回，系統正確進入下一段十空考試題面。

十格可輸入版驗證：檢討題面顯示 1–10 共十個可直接輸入的拼寫欄。第一格輸入錯誤首字母 `x` 時，字元立即被移除並提示「應輸入 t」；輸入完整正解 `the` 後，該格顯示正確，焦點自動移至下一未完成格。以鍵盤輸入後核對 `document.activeElement`，確認焦點移至第 4 次練習欄，無需滑鼠。

錯字保留修正後，第一格輸入 `x` 仍保留字元於該格，欄位以錯誤樣式顯示，並即時提示「第 1 格的第 1 個字母不正確；應輸入 t」。即使該錯字保留且尚未完成任一練習格，仍可直接選擇 `RETURN TO PASSAGE`，系統成功進入下一段十空考試題面。

非揭露提示修正後，第一格輸入 `x` 時仍保留該字元與錯誤樣式；提示僅顯示「第 1 格有拼寫錯誤，請自行檢查並修正」，不含正確字母或錯誤位置等答案線索。

文案與純白介面修正後，側欄顯示 `Practice makes progress.`／「每一次練習，都讓進步看得見。」；檢討標題已改為「自由拼寫練習區」。第一格輸入正解後，該格與其他格均維持白色背景、一般深色文字，未再顯示綠色底、綠色正確字或淺色提醒。

主題面缺字數遮蔽修正後，第一段十個填空欄均採固定相同寬度，不再依缺失字母長度伸縮。第一空輸入六個字母 `abcdef` 後仍完整保留輸入內容，確認欄位已無 `maxLength` 造成的輸入長度提示；系統僅在提交時依完整答案判定正誤。

題組輪替與續作修正後，主題面明確顯示「題組 01 · 1/84」。按下 NEW ROUND 後，載入不同內容的「題組 11 · 11/84」；重新載入頁面後仍停留於題組 11，確認目前題組游標已寫入本機進度且不會回到前兩組。

高錯題組間隔複習驗證：手動建立題組 01 的待複習狀態，並將連續新題數設定為 4。完成題組 11 後，系統自動切換至「高錯複習題組 01」，畫面顯示待複習題組數與「完成本段後將回到原本的新題順序」說明。完成複習題組 01 的錯題檢討後，系統回到題組 12，確認未打亂84組主線。

自由拼寫答案提示移除後，錯題 `electricity` 的十個練習輸入欄在未輸入時僅顯示格號與空白底線；網頁可及性資料也不含 placeholder 值。沒有任何淡色 `electricity` 或其他答案字母顯示於練習格中。

跳過整段檢討驗證：錯題檢討頁顯示 `SKIP REVIEW · NEXT PASSAGE`。在不輸入任何自由拼寫格的情況下按下後，畫面直接切換為題組 13 的十空考試題面；未清除既有錯題統計與待複習題組資料。

跳過操作置底驗證：檢討頁先出現完整句子、十格自由拼寫區、`NEXT MISTAKE` 與 `REVIEW WORD`；其後才出現獨立底部區塊「不想練習也可以直接前往下一段」與 `SKIP REVIEW · NEXT PASSAGE／跳過本次檢討` 按鈕。此操作不再與下一錯題並列於主操作列。

每日兩組引導驗證：將當日完成數設為 1，完成下一段並跳過該段錯題檢討後，介面停在「今日建議的 2 組已完成」頁面，沒有自動進入下一段。該頁提供「繼續新題」「複習高錯題」及「今天先到這裡」三個操作。選擇「今天先到這裡」後，畫面顯示今日練習已結束；重新載入後，此完成狀態與下一題組續作提示仍會保留。

每日提示可見性強化驗證：每日完成數為 0 時，第一段主題面在題組資訊前即顯示「DAILY TARGET／每日建議」「今天建議完成 2 組」與 00／02 組。完成數為 1 時，提示顯示「已完成 1/2 組，還差 1 組」。完成第2組且跳過檢討後，系統立即停在 2/2 完成選擇畫面，第三組未被自動載入。
