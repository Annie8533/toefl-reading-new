# 純前端解鎖實測

1. 開啟首頁時，實際顯示解鎖輸入框與兩種範圍：`DEMO2026`（2 題）及 `TOEFL521`（84 題）。
2. 在頁面輸入 `DEMO2026` 並送出後，實際進入考試題面；頁首顯示 `DEMO · 02 PASSAGES`、題組顯示 `2/2`，頁尾亦顯示 `2 PASSAGES`，確認體驗模式只有兩題。
3. 實際點選 `CHANGE CODE` 回到解鎖頁，輸入 `TOEFL521` 並送出後，頁首顯示 `FULL · 84 PASSAGES`、題組顯示 `6/84`、頁尾顯示 `84 PASSAGES`，確認完整模式載入 84 題題庫。
